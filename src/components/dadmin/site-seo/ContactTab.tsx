"use client";

import * as React from "react";

/**
 * Temporary stub to satisfy SiteSeoForm import and restore build.
 * Replace with real fields in the next task.
 */
export default function ContactTab(props: any) {
  // If the parent passes form controls, keep them wired but render simple content for now
  return (
    <div className="space-y-4">
      <p className="text-sm text-muted-foreground">
        Contact settings will be configured here (stub).
      </p>
    </div>
  );
}
