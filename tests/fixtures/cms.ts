
export async function maybeSeedCMS() {
  // If Studio allows running the seed script, do it once before tests.
  // Otherwise, no-op. Keep as placeholder for future toggles.
  // Example (commented): await execa('npm', ['run', 'cms:seed']);
}
