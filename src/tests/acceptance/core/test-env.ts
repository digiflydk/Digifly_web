// src/tests/acceptance/core/test-env.ts

export type AcceptanceEnv = {
  // placeholder for future shared helpers, e.g. cmsClient, firestoreClient, etc.
};

export function initAcceptanceEnv(): AcceptanceEnv {
  // For now this is just a stub to prove the wiring works.
  // Later tasks will extend this with real environment setup.
  return {};
}
