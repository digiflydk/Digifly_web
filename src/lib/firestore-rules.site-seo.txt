match /databases/{db}/documents {
  match /settings/site-seo {
    allow read: if true;                  // offentligt læs (ok til metadata)
    allow write: if request.auth != null; // kun signed-in admin i Studio
  }
}
